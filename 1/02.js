// there are some tricky que
// when we compare two different datatype
// console.log("2" > 1); //true
// console.log("02" > 1); //true

// for null
// console.log(null > 0);
// console.log(null < 0);
// console.log(null == 0);
// console.log(null >= 0);
// main reaon being when we use comparison op, null is convertd to 0
// which is not the case with ==

// === also check the datatype
// console.log("2" === 2);

// -------------------
// stack(all primitive) heap(all non-primitive)
// in stack memory we get call by value
// whereas in heap we get call by refrence
